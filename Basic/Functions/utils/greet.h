//Prevents compilier from compiling twice
#ifndef UTILS_GREET
#define UTILS_GREET

#include <string>

namespace messaging {
    //* Function declaration (function prototype), unlike defining functions
    void greet(std::string name);
}



#endif